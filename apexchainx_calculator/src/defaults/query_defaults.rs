//! Canonical default values returned by public query methods.

/// Default history retention.
pub const DEFAULT_HISTORY_LIMIT: u32 = 0;

/// Default protocol version.
pub const DEFAULT_PROTOCOL_VERSION: u32 = 1;

/// Default telemetry enabled flag.
pub const DEFAULT_TELEMETRY_ENABLED: bool = false;

/// Default governance epoch.
pub const DEFAULT_GOVERNANCE_EPOCH: u64 = 0;

/// Default pause state.
pub const DEFAULT_PAUSED: bool = false;